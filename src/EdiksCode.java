import java.util.HashMap;
import java.util.Map;

/**
 * Created by user on 25.02.2017.
 */
public class EdiksCode {
   /** String caseB = "b";//1
    String caseA = "a";//2

    HashMap<Integer, Map<String, Integer>> longSetHashMap = new HashMap<>();



        longSetHashMap.put(156, "a");

        longSetHashMap.entrySet().forEach(pointForAnswerByQuestionNumber -> {
        Integer questionNumber = pointForAnswerByQuestionNumber.getKey();
        String answer = arrayOfTestDataForOnePerson[questionNumber];
        Integer pointForAnswer = pointForAnswerByQuestionNumber.getValue().getOrDefault(answer, 0);
        ScalesOfKettelsTest scale = ScalesOfKettelsTest.E;
        Integer currentPoints = valuesOfTestScalesForOnePerson.getOrDefault(scale, 0);
        valuesOfTestScalesForOnePerson.put(scale, currentPoints + pointForAnswer);
    });
*/
}
